# Markdown 速查指南

一份简明扼要的 Markdown 语法指南，涵盖标题、列表、链接、表格等常用格式。

## 1. 标题

用 `#` 的个数表示层级，最多六级：

```markdown
# 一级标题
## 二级标题
### 三级标题
```

## 2. 强调

```markdown
**粗体**  *斜体*  ~~删除线~~  `行内代码`
```

## 3. 列表

无序用 `-`，有序用 `1.`，缩进两个空格可嵌套：

```markdown
- 第一项
- 第二项
  - 子项

1. 第一步
2. 第二步
```

任务列表：

```markdown
- [ ] 未完成
- [x] 已完成
```

## 4. 链接与图片

```markdown
[链接文字](https://example.com)
![图片说明](images/photo.jpg)
```

## 5. 表格

```markdown
| 列A | 列B | 列C |
|---|---|---|
| 值1 | 值2 | 值3 |
```

## 6. 引用

```markdown
> 引用块：常用来摘录原文或引言。
```

## 7. 代码块

三个反引号包围，可标注语言：

````markdown
```python
print("hello")
```
````

## 8. 分隔线与换行

```markdown
---            （三个短横线 = 分隔线）

行尾加两个空格  
即可强制换行。
```

## 9. 写 skill 的关键：frontmatter

skill 文件顶部用 `---` 包起来的 YAML 元数据，openocde 靠它识别：

```markdown
---
name: my-skill
description: 一句话说清这个 skill 何时使用。
---
```

## 常用语法对照

| 语法 | 写法 | 常见用途 |
|---|---|---|
| 标题 | `#` 到 `######` | 组织文档层级 |
| 强调 | `**粗**`、`*斜*`、`` `代码` `` | 突出关键词、代码 |
| 列表 | `-`、`1.` | 步骤、条目 |
| 链接/图片 | `[文字](url)`、`![说明](path)` | 引用出处、贴图 |
| 表格 | `\|` 与 `---` | 对比数据 |
| 引用 | `>` | 摘录原文 |
| 代码块 | 三反引号 | 贴命令、脚本 |
